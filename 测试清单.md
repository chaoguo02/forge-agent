# Forge Agent 测试清单

> 项目共有 **509 个测试用例、17 个测试文件**，当前运行结果 **509 passed**。

---

## 一、测试文件总览

| 文件 | 描述 | 状态 |
|------|------|------|
| `tests/test_day2.py` | Agent 运行循环：Finish, GiveUp, MaxSteps, 循环检测, 反思, LLM错误 | ✅ |
| `tests/test_day4.py` | LLM 后端：Anthropic, OpenAI-compat, 文本回退, Prompt 构建, Router | ✅ |
| `tests/test_chat.py` | Chat 会话：多轮聊天、历史持久化、模式切换 | ✅ |
| `tests/test_confirm.py` | Shell 确认机制：只读/危险命令识别、回调、Agent 流程 | ✅ |
| `tests/test_factory.py` | Agent 工厂：React/Plan 创建、自动模式、复杂任务判定 | ✅ |
| `tests/test_plan.py` | Plan 解析（JSON/Markdown）、Plan-and-Execute Agent、自动/审批模式 | ✅ |
| `tests/test_dag.py` | DAG 并行执行模式 | ✅ |
| `tests/test_multi_agent.py` | Multi-Agent 协调：角色规范化、spawn 限制、工具集成 | ✅ |
| `tests/test_skills.py` | Skill 系统：发现、加载、渲染、SkillTool | ✅ |
| `tests/test_web_tool.py` | Web 工具：URL 验证、WebSearch, WebFetch, 内容提取 | ✅ |
| `tests/test_compaction.py` | 上下文压缩、摘要生成 | ✅ |
| `tests/test_code_chunker.py` | 代码分块切割 | ✅ |
| `tests/test_memory_store.py` | 记忆存储（持久化、检索） | ✅ |
| `tests/test_memory_tool.py` | 记忆工具调用 | ✅ |
| `tests/test_memory_system.py` | 记忆系统集成 | ✅ |
| `tests/test_rag_memory.py` | RAG 记忆检索 | ✅ |
| `tests/test_native_tool_use.py` | Native tool use 格式解析 | ✅ |

---

## 二、已删除的稳定测试文件（系统成熟后不再需要）

| 文件 | 原因 |
|------|------|
| `test_day1.py` | 核心数据结构（Task/Action/Observation）已稳定 |
| `test_day3.py` | 文件/Shell/搜索工具已稳定 |
| `test_day5.py` | TokenBudget/RepoMap/History 基础功能已稳定 |
| `test_day6.py` | Config/CLI 加载已稳定 |
| `test_day7.py` | 重试/Git Diff/集成测试（依赖外部） |
| `test_sandbox.py` | Docker 沙箱（需要 Docker 环境） |
| `test_renderer.py` | TUI 渲染输出（视觉验证） |
| `test_stream.py` | 流式输出（已稳定） |

---

## 三、运行测试命令

```bash
# 全量测试（~50s）
pytest tests/

# 快速验证
pytest tests/ -q --tb=short

# 单个文件
pytest tests/test_multi_agent.py -v

# 只看失败
pytest tests/ -x --tb=short
```
